name: Gerar Documentação

on:
  workflow_dispatch:
    inputs:
      mensagem_commit:
        description: 'Mensagem para o commit que será usado no CHANGELOG.'
        required: true
        default: 'docs: atualização automática da documentação'

jobs:
  gerar-documentacao:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do Repositório
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configurar Ambiente Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Instalar Dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Instalar Readocs em modo desenvolvimento
          pip install -e .

      - name: Executar Agentes de Documentação
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}  # Nova variável!
          COMMIT_MESSAGE: ${{ github.event.inputs.mensagem_commit }}
        run: |
          python -m readocs generate

      - name: Revisar e Fazer o Commit das Mudanças
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          if git diff --quiet HEAD -- README.md CHANGELOG.md; then
            echo "📝 Nenhuma mudança detectada"
          else
            echo "📝 Commitando mudanças..."
            git add README.md CHANGELOG.md
            git commit -m "${{ github.event.inputs.mensagem_commit }}"
            git push
            echo "✅ Mudanças commitadas"
          fi